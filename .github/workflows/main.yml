on:
  push:
    # Pattern matched against refs/tags
    tags:        
      - 'v*' # Push events to every tag not containing /

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: curl -X POST -d {} https://api.vercel.com/v1/integrations/deploy/prj_YUJgz9yOxU9TpuyORSiiANjezDLU/xA4ofZfptL
